server.error.include-message=always

spring.datasource.url=jdbc:mysql://localhost:3306/bookify?useUnicode=true&characterEncoding=utf-8
spring.jpa.hibernate.ddl-auto=update
spring.datasource.username=root
spring.datasource.password=ftnns
spring.cache.type=none

image.path=${IMAGES_PATH}
spring.servlet.multipart.max-file-size=500MB
spring.servlet.multipart.max-request-size=500MB

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=bookify.team3@gmail.com
spring.mail.password=qgbmdoniwhfxmnra
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=*

activation.cron=0 */1 * ? * *

server.address=0.0.0.0

server.ssl.key-store=${SECURITY_PATH}/https.p12
server.ssl.key-store-password=https
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=https

server.port=8082

spring.security.oauth2.client.registration.keycloak.client-id=login-app
spring.security.oauth2.client.registration.keycloak.authorization-grant-type=authorization_code
spring.security.oauth2.client.registration.keycloak.scope=openid

spring.security.oauth2.client.provider.keycloak.issuer-uri=http://localhost:8080/realms/SpringBootKeycloak
spring.security.oauth2.client.provider.keycloak.user-name-attribute=preferred_username
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8080/realms/SpringBootKeycloak
spring.security.oauth2.client.registration.keycloak.redirect-uri=https://localhost:4200/*

##spring.ldap.embedded.ldif=src/main/resources/server.ldif
#spring.ldap.embedded.base-dn=dc=example,dc=com
#spring.ldap.embedded.port=10389

# LDAP URL to the existing LDAP server
spring.ldap.urls=ldap://localhost:10389

# Base DN for LDAP searches
spring.ldap.base=ou=system

# User DN and password for authentication (if required)
spring.ldap.username=uid=admin,ou=system
spring.ldap.password=secret

# Optionally, specify the search filter for user authentication
#spring.ldap.base-roles-dn=ou=groups,dc=example,dc=com
#spring.ldap.user-dn-pattern=uid={0},ou=users